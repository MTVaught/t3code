/**
 * Treher owns a separate home from upstream T3 Code. State keeps the existing
 * `userdata` layout inside that root so explicit base-directory overrides have
 * unsurprising semantics.
 */
export const DEFAULT_TREHER_HOME_DIRECTORY_NAME = ".t3-treher";
export const PRODUCTION_STATE_DIRECTORY_NAME = "userdata";
